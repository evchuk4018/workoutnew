import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MacroWeb - Track Your Nutrition, Transform Your Life",
  description: "Personalized macro tracking with AI-powered food recognition. Set your goals, log your meals, and watch your progress with weekly intelligent adjustments.",
  keywords: ["nutrition", "macro tracking", "diet", "weight loss", "fitness", "calories", "meal planning"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
